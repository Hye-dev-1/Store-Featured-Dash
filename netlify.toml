[build]
  publish = "public"
  functions = "netlify/functions"

# /api/crawl → function
[[redirects]]
  from = "/api/crawl"
  to = "/.netlify/functions/crawl"
  status = 200
  force = true

# SPA fallback (must be last)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
